# Core numerical and medical-image stack
numpy>=1.24,<3
scipy>=1.10,<2
SimpleITK>=2.3,<3

# Supervised refinement models
torch>=2.1,<3

# Folder-level surface metrics
medpy>=0.5,<1

# Optional integrations are intentionally not installed here:
# - PyMaxflow enables the max-flow solver in utils/geometry.py.
# - SAM-Med3D and its segment_anything package must be installed from
#   their upstream repository under the upstream license.
